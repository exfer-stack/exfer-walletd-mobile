# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri mobile (`tauri android init` / `tauri ios init`).
# Regenerated in CI on each build. Commit this directory only once you
# customize the native projects (manifest permissions, signing config,
# launch screen) and want those changes to persist — see docs/RELEASE.md.
/gen
